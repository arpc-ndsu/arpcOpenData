% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_bundle.R
\name{fetch_bundle}
\alias{fetch_bundle}
\title{Download and extract a published data bundle, with on-disk caching.}
\usage{
fetch_bundle(
  id,
  dest = NULL,
  repo = "arpc-ndsu/arpcOpenData",
  cache_dir = tools::R_user_dir("arpcOpenData", "cache"),
  force = FALSE,
  verify = TRUE
)
}
\arguments{
\item{id}{Bundle ID, e.g. \code{"data-arpc-report-202601"}.}

\item{dest}{Directory to extract into. Defaults to the cache directory.}

\item{repo}{GitHub "owner/name" hosting the releases.}

\item{cache_dir}{Directory for cached ZIPs (persists across sessions).}

\item{force}{If TRUE, re-download and re-extract even if cached.}

\item{verify}{If TRUE (default), check extracted files against the manifest.}
}
\value{
Path to the extracted bundle directory (invisibly).
}
\description{
Fetches \verb{data-arpc-<TYPE>-<YYYY><##>.zip} from its publication-type GitHub
Release and unpacks it locally. The ZIP is cached, so repeat calls do not
re-download unless \code{force = TRUE}; likewise an already-extracted bundle is not
re-unzipped. By default the extracted files are verified against the bundle's
\verb{_manifest.json} sha256 checksums.
}
\examples{
\dontrun{
  dir <- fetch_bundle("data-arpc-report-202601")
  list.files(dir)
  df <- read.csv(file.path(dir, "arpc-report-202601-figure001.csv"))
}
}
