% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_public_data.R
\name{build_bundle}
\alias{build_bundle}
\title{Build a public data bundle for one ARPC publication.}
\usage{
build_bundle(
  type,
  year,
  issue,
  title,
  pdf,
  exhibits,
  out_root = file.path("data-raw", "published-data"),
  meta = list()
)
}
\arguments{
\item{type, year, issue}{Publication coordinates (type in VALID_TYPES).}

\item{title}{Human-readable publication title.}

\item{pdf}{Path to the final publication PDF.}

\item{exhibits}{Ordered list of exhibits. Each is a list with:
kind        = "figure" or "table"
data        = a data.frame (or path to .rds)
image       = path to PNG          (figures only)
description = one-line exhibit description
dict        = optional named list of column overrides}

\item{out_root}{Staging directory for built bundles.}

\item{meta}{Named list merged into the manifest (source_repo,
source_commit, publication_date, program_area, ...).}
}
\description{
Assembles a publication's exhibits into a self-contained, citable bundle
(\verb{data-arpc-<TYPE>-<YYYY><##>}): one CSV per figure/table with a codebook each,
the figure PNGs, the publication PDF, a checksummed \verb{_manifest.json}, and an
exhibit-map \code{README.md}. Files are written under \verb{out_root/<TYPE>/<ID>/}.
}
